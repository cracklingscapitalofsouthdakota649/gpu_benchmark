# requirements.txt (shared, framework-free)
pytest
pytest-benchmark
allure-pytest
psutil
matplotlib
nvidia-ml-py
amdsmi
pygments
pluggy
# (No torch/vision/audio here; installed by script per backend)
# (No intel-extension-for-pytorch on Windows; installed only on Linux/WSL runner)